name: Test

on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: voxpelli/ghatemplates/.github/workflows/simple-test.yml@main
